apiVersion: v1
kind: Secret
metadata:
  name: momento-secrets
  namespace: momento-v5
type: Opaque
stringData:
  # Database Secrets
  POSTGRES_USER: "changeme"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_REPLICATION_PASSWORD: "changeme"
  REDIS_PASSWORD: "changeme"
  
  # Application Secrets
  JWT_SECRET: "changeme"
  API_SECRET_KEY: "changeme"
  ENCRYPTION_KEY: "changeme"
  
  # External Service Secrets
  AWS_ACCESS_KEY_ID: "changeme"
  AWS_SECRET_ACCESS_KEY: "changeme"
  AWS_REGION: "us-east-1"
  
  # Monitoring Secrets
  PROMETHEUS_USERNAME: "changeme"
  PROMETHEUS_PASSWORD: "changeme"
  GRAFANA_ADMIN_PASSWORD: "changeme"
  
  # Certificate Secrets (to be replaced with actual certificates)
  TLS_CERT: ""
  TLS_KEY: ""
