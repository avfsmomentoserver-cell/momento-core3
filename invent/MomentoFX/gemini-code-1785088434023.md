## VII. Deep DNA Similarity & High-Target Pattern Matching

When the user specifies an extreme target multiplier (e.g., $T = 2000$), the system must dynamically shift from generalized pressure tracking to **Targeted DNA Similarity Matching**[cite: 1]. This engine identifies the current market state and compares it against historical precursors of identical rare events.

### 1. Dynamic Boundary Definition
Instead of looking for exact matches, Devin must implement a variance window to capture a statistically significant sample size of historical events.
*   **Target ($T$)**: The user-defined goal (e.g., $2000$).
*   **Variance Window ($\Delta$)**: Dynamically set to 50% of the target (e.g., $\pm 1000$).
*   **Query Range**: Retrieve all historical rounds where $T - \Delta \le \text{Multiplier} \le T + \Delta$.

### 2. State Vector Extraction (The "DNA" Signature)
For every historical event $i$ found in the query range, the engine must extract a historical state vector $V_i$ representing the $N$ rounds (e.g., $N=500$) immediately preceding the event. The real-time data ingestion feed[cite: 1] will continuously generate a current state vector $V_{current}$.

The vector must contain the following Momento Intelligence factors:

$$ V = [D_b, \Sigma M, L_c, P_{ceiling}] $$

Where:
*   $D_b$ (**Band Distribution**): A normalized sub-vector representing the frequency of MomentoLinguistics semantic tokens (Ignition, Moonshot, Mega)[cite: 1] in the preceding $N$ rounds.
*   $\Sigma M$ (**Multiplier Sum**): The total accumulated raw multiplier energy leading up to the event.
*   $L_c$ (**Ladder Count**): The integer count of detected ladder structures (consecutive stepping sequences)[cite: 1] prior to the breakout.
*   $P_{ceiling}$ (**Accumulated Ceiling Pressure**): The count of "failed breakouts"—rounds that entered the Mega band (50x-100x) but crashed before crossing into the Cosmic/Galactic bands. This acts as a quantifiable "boiling point" metric.

### 3. Similarity Calculation Algorithm
To find how closely the current live market matches historical precursors, Devin must implement a Cosine Similarity algorithm to compare $V_{current}$ against all historical $V_i$ vectors in the target database.

$$ \text{Similarity}(V_{current}, V_i) = \frac{V_{current} \cdot V_i}{\|V_{current}\| \|V_i\|} = \frac{\sum_{k=1}^{n} A_k B_k}{\sqrt{\sum_{k=1}^{n} A_k^2} \sqrt{\sum_{k=1}^{n} B_k^2}} $$

*   **Match Threshold**: Any historical event $i$ where $\text{Similarity} \ge 0.85$ is classified as a "DNA Match"[cite: 1].

### 4. ETA Synthesis & Intelligent Output
Once DNA matches are identified, the Forecast Engine[cite: 1] must generate the ETA and insights based on how those specific historical patterns resolved:

1.  **Time-to-Eruption (ETA)**: Calculate the average gap (in rounds) from the moment the historical vector reached an $0.85$ similarity score to the actual occurrence of the $T \pm \Delta$ event. 
2.  **Factor Highlighting**: The UI must explicitly highlight which metric is driving the similarity. (e.g., *"Current environment shows a 92% match to the April 14th 2200x event, heavily driven by identical Accumulated Ceiling Pressure ($P_{ceiling}$) and Ladder Count ($L_c$)."*)
3.  **Confidence Scoring**: The statistical confidence level provided in the output must scale directly with the number of historical DNA matches found (e.g., matching 5 historical events yields higher confidence than matching only 1)[cite: 1].

### 5. Implementation Directives for Devin
*   **Database**: Utilize the `/api/v1/rounds/all` endpoint[cite: 1] to ensure the dataset is large enough (up to 100,000 rounds) to find these rare target occurrences.
*   **Compute Isolation**: Vector math (Cosine Similarity) over large arrays is computationally heavy. Devin must ensure this calculation is memoized in React Query or executed on the Python backend using NumPy to prevent blocking the main FastAPI thread or freezing the frontend UI.

chat with gemini

# `DEVIN_MEGA_PRESSURE.md` - Mega Pressure Tracker AI Developer Blueprint

## Executive Directives for Devin

When instructed to build, extend, or optimize the Mega Pressure Tracker within the Momento Core ecosystem, you must adhere strictly to this blueprint. This document defines the exact mathematical models, architectural constraints, and strategic intelligence required to build a production-grade Mega Pressure Tracker.

As an AI engineer, you must anchor your development in the four foundational principles of Momento Core:

* **Observation Before Prediction**: Understand the present data state before generating forecasts.


* **Immutable Raw Events**: Never alter raw multiplier data; log any corrections independently.


* **Explainability Is Mandatory**: Ensure all generated ETA and range predictions carry detailed metadata explaining the calculation.


* **Local vs Production Independence**: Ensure the backend functions entirely on a local SQLite database (in WAL mode) before porting to any cloud infrastructure.



---

## I. Architectural Constraints & Pipeline

Mega Pressure Tracker must operate strictly as a downstream module within the established Momento Core pipeline: **Collector → Ingest API → Analysis → Forecast Engine → Database → Dashboard**.

### Integration Map

1. **Data Ingestion Layer**: Connect to `/api/v1/rounds/all` (handling up to **100,000** rounds) and `/api/v1/sources` to fetch historical crash curves.


2. **Semantic Layer**: Utilize `/api/v1/linguistics` to categorize raw multipliers into the eight-layer semantic vocabulary (e.g., Ignition, Moonshot, Mega, Cosmic).


3. **Analytics Middleware**: Build `megaPressure.ts` strictly isolated from the main system, passing structured data to the React UI via the middleware pattern.


4. **Forecast Registration**: The forecast engine must submit all ETA and probability predictions to `/api/v1/forecasts` **before** the actual round lands to comply with the platform's honest accuracy scoring methodology.



---

## II. Mathematical Intelligence & Strategic Modeling

You must translate the following quantitative models into Python (FastAPI backend) and TypeScript (React Query middleware) logic.

### 1. The Pressure Index Algorithm

The Mega Pressure Tracker relies on a multivariate metric $P(t)$ mapped to a **0.0** to **1.0** scale.

$$P(t) = w_E E(t) + w_S S(t) + w_M M(t) + w_D D(t)$$

Where:

* $E(t)$ is **Energy Buildup**: The cumulative sum of recent multipliers relative to the historical average energy between Mega events.
* $S(t)$ is **Shape Consistency**: Pattern recurrence matched against the Intelligence Engine Chain (Pattern → DNA → Similarity).


* $M(t)$ is **Band Momentum**: The velocity of transition across semantic bands (e.g., frequency of Moonshots leading up to a Mega).
* $D(t)$ is **Time Decay**: The pressure decay or inflation adjusted for the time elapsed since the last Mega.
* $w_i$ are dynamic variables tied to the live weight management via the Plugin Inventory.



### 2. ETA Forecasting (Survival Analysis)

Do not rely purely on simple averages. Implement a Kaplan-Meier estimator or Weibull distribution to calculate the hazard rate $h(t)$ of a Mega event occurring at round $t$, given it has not yet occurred:

$$h(t) = \frac{f(t)}{1 - F(t)}$$

Combine this hazard rate with the current pressure $P(t)$ to generate an adjusted Expected Time of Arrival (ETA). Provide statistical confidence bands based on the standard error:

$$CI = \mu_{ETA} \pm Z \left( \frac{\sigma}{\sqrt{n}} \right)$$

### 3. Bankroll & Chase Strategy Intelligence

The tracker must output mathematically sound, risk-managed betting sequences to generate intelligent chase strategies.

* **The 1% Bankroll Rule**: Hardcode conservative logic where the base bet does not exceed **1%** of the total capital to survive extreme variance.
* **Geometric Bet Progression**: For aggressive or recovery strategies, implement a defined multiplier growth rate $r$ to manage sequences:

$$b_k = b_1 \cdot r^{k-1}$$


* **Expected Value (EV) Guardrails**: Ensure every proposed sequence calculates its Expected Value to prevent irrational chases. Stop-loss parameters must be derived from EV:

$$EV = (P_{win} \cdot \text{Target Multiplier}) - (P_{loss} \cdot \text{Cumulative Wager})$$



---

## III. Code Implementation Specifications

### Backend Extensions (Python / FastAPI)

You must use Python 3, FastAPI, and async/await for I/O operations. Enforce PEP 8 formatting, include Google-style docstrings, and demand explicit type hints for all function signatures.

```python
from fastapi import APIRouter, Depends
from pydantic import BaseModel
from typing import List, Optional

router = APIRouter(prefix="/api/v1/mega-pressure", tags=["pressure"])

class PressureMetrics(BaseModel):
    current_pressure: float
    avg_mega_gap: float
    energy_buildup: float
    time_decay: float
    confidence_score: float

@router.get("/metrics", response_model=PressureMetrics)
async def get_pressure_metrics(source: str, limit: int = 1000) -> PressureMetrics:
    """
    Computes real-time pressure metrics using the weighted algorithm.
    """
    # Devin: Implement DB fetch via SQLAlchemy ORM (SQLite WAL mode)[cite: 1]
    # Map raw events through MomentoLinguistics vocabulary
    pass

```

### Frontend Middleware (React / TypeScript)

You must write functional components with strict TypeScript definitions and hooks, using JSDoc for documentation. Avoid the `any` type without written justification.

```typescript
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';

/**
 * Interface representing the output of the ETA Hazard Model.
 */
export interface ETAPrediction {
    rounds_eta: number;
    time_eta_minutes: number;
    confidence_50: { min: number; max: number };
    confidence_95: { min: number; max: number };
}

/**
 * Fetches the pressure-adjusted ETA forecast.
 * @param source The active data source identifier.
 */
export function useETAPrediction(source: string) {
    return useQuery<ETAPrediction>({
        queryKey: ['etaPrediction', source],
        queryFn: () => api.get(`/api/v1/mega-pressure/eta?source=${source}`),
        refetchInterval: 10000, 
        staleTime: 10000,
    });
}

```

---

## IV. Honest Accuracy & System Validation

You must implement rigid backtesting and verification protocols for the Mega Pressure Tracker.

1. **Strict Forward-Testing**: The Forecast Engine must log its $P(t)$ triggers and ETA predictions into the SQLite database *before* the crash curve result is resolved; unrecorded forecasts do not count toward system accuracy.


2. **Brier Scoring**: Validate the accuracy of probabilistic Range Predictions utilizing a Brier Score metric to verify real predictive performance.



$$BS = \frac{1}{N} \sum_{t=1}^{N} (f_t - o_t)^2$$


3. **Local Replay Mode**: Build a script utilizing the `backend/data/inbox/` file watcher to stream historical JSON/CSV arrays as if they were live, verifying that UI components update properly via the `/ws` WebSocket endpoint.



---

## V. Devin Task Execution Roadmap

When initialized to work on this specific module, proceed in this exact sequence:

1. **Environment Sync**: Run `python3 run_api.py --init-only` to instantiate the SQLite database.


2. **Endpoint Scaffolding**: Build the FastAPI router for `/api/v1/mega-pressure` mapping to the quantitative models above.
3. **Middleware Construction**: Develop `web/src/lib/invent-middleware/megaPressure.ts` with strict React Query caching rules.


4. **UI Development**: Construct `MegaPressureTracker.tsx` utilizing TailwindCSS for layout and Recharts for data visualization.


5. **Quality Gates**: Execute `bun run lint` and `python3 -m compileall -q momento run_api.py` to ensure syntactical perfection before considering the task complete.



---

Are there any specific semantic layers (e.g., Cosmic vs. Galactic bands) you want Devin to prioritize when calculating the probability distribution bounds for the next predicted event?